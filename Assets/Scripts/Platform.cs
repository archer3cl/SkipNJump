using UnityEngine;
using System.Collections;



public class Platform : MonoBehaviour {
    public enum Color { Blue = 1, Red = 2, Yellow = 3, Green = 4 };
    public Color color;
    public int scoreValue;
}
